#!groovy

node {
  stage 'Checkout'
  
  //Checkout latest build from the repo
  checkout scm
  
  stage 'Build'
  //Build
  echo "This is where a build happens."
  
  stage 'stage3'
  echo "Example stage 3."
}
